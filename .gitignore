__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

raw.json
report.json
!fixtures/demo.json
!data/reports/report.json

# Submission evidence. The bare `report.json` rule above matches at any
# depth, which would silently drop the agent's own run outputs. Agent run
# directories and the measured comparison are deliverables: keep them.
!artifacts/runs/**/report.json
!artifacts/evaluation/**/report.json

node_modules/
web/dist/
web/.vite/

.env
.env.local
*.log
.DS_Store
